# Инструкции по установке функций управления паролями

## Что было добавлено

Система управления паролями с криптостойкой генерацией и логированием.

## Файлы, которые были изменены

### 1. Frontend (HTML/JavaScript)
**Файл:** `skkp_project/dashboard/templates/dashboard/employee_form.html`

**Изменения:**
- Добавлена кнопка 🔐 "Сгенерировать пароль"
- Добавлена кнопка 👁️ "Показать/скрыть пароль"
- Добавлена кнопка 🔄 "Сбросить пароль"
- Добавлены JavaScript функции для управления паролями
- Добавлено логирование в localStorage

### 2. Backend (Django)
**Файл:** `skkp_project/dashboard/views.py`

**Добавлено:**
- Функция `log_password_generation()` для логирования на сервере
- Отправка логов в Django LogEntry для аудита

### 3. URL маршруты
**Файл:** `skkp_project/dashboard/urls.py`

**Добавлено:**
- Маршрут `api/log-password-generation/` для API логирования

### 4. Конфигурация
**Файл:** `skkp_project/config/settings.py`

**Добавлено:**
- Конфигурация LOGGING для ротирующихся логов
- Автоматическое создание папки `logs/`

## Требования

- ✅ Django 5.2+
- ✅ Python 3.8+
- ✅ Bootstrap 5.3.0
- ✅ Bootstrap Icons 1.11.0
- ✅ Современный браузер с поддержкой Web Crypto API

## Установка

### Шаг 1: Обновить файлы
Все файлы уже обновлены. Просто убедитесь, что:
- `employee_form.html` содержит кнопки для управления паролями
- `views.py` содержит функцию `log_password_generation()`
- `urls.py` содержит маршрут для логирования
- `settings.py` содержит конфигурацию LOGGING

### Шаг 2: Создать папку логов
```bash
mkdir -p logs
chmod 755 logs
```

### Шаг 3: Перезагрузить Django
```bash
python manage.py runserver
```

### Шаг 4: Проверить работу
1. Откройте форму создания сотрудника
2. Введите логин
3. Нажмите кнопку 🔐 "Сгенерировать пароль"
4. Проверьте, что пароль заполнился
5. Откройте консоль браузера (F12)
6. Выполните: `JSON.parse(localStorage.getItem('admin_password_logs'))`
7. Должны увидеть логи генерации

## Проверка установки

### Проверка frontend
```javascript
// В консоли браузера
typeof generateSecurePassword  // должно быть "function"
typeof logPasswordGeneration   // должно быть "function"
```

### Проверка backend
```bash
# Проверить, что маршрут существует
python manage.py show_urls | grep log-password

# Проверить, что логи создаются
tail -f logs/admin_password_log.log
```

### Проверка конфигурации
```bash
# Проверить, что папка логов существует
ls -la logs/

# Проверить права доступа
stat logs/
```

## Использование

### Создание сотрудника с автогенерированным паролем

1. **Откройте форму:**
   ```
   Панель управления → Сотрудники → Добавить сотрудника
   ```

2. **Заполните логин:**
   ```
   Логин: wb-worker-1
   ```

3. **Сгенерируйте пароль:**
   - Нажмите кнопку 🔐 "Сгенерировать пароль"
   - Пароль автоматически заполнится
   - Пароль скопируется в буфер обмена
   - Покажется уведомление с паролем

4. **Создайте сотрудника:**
   - Нажмите кнопку "Создать сотрудника"
   - Пароль будет залогирован

### Просмотр логов

**В браузере:**
```javascript
// Открыть консоль (F12)
JSON.parse(localStorage.getItem('admin_password_logs'))
```

**На сервере:**
```bash
# Просмотр логов в реальном времени
tail -f logs/admin_password_log.log

# Поиск по сотруднику
grep "wb-worker-1" logs/admin_password_log.log

# Количество генераций
grep "password_generated" logs/admin_password_log.log | wc -l
```

## Устранение неполадок

### Проблема: Пароль не генерируется
**Решение:**
1. Проверьте консоль браузера (F12) на ошибки
2. Убедитесь, что браузер поддерживает Web Crypto API
3. Проверьте, что JavaScript включен

### Проблема: Логи не сохраняются
**Решение:**
1. Проверьте, что папка `logs/` существует
2. Проверьте права доступа: `chmod 755 logs/`
3. Проверьте, что Django может писать в папку
4. Проверьте логи Django: `python manage.py runserver`

### Проблема: Пароль не копируется
**Решение:**
1. Убедитесь, что сайт работает по HTTPS (требование безопасности)
2. Проверьте, что браузер поддерживает Clipboard API
3. Проверьте консоль браузера на ошибки

## Конфигурация

### Изменить длину пароля
**Файл:** `employee_form.html`
```javascript
const length = 16;  // Измените на нужное значение
```

### Изменить набор символов
**Файл:** `employee_form.html`
```javascript
const charset = 'ABCDEFGHIJKLMNOPQRSTUVWXYZabcdefghijklmnopqrstuvwxyz0123456789!@#$%^&*()_+-=[]{}|;:,.<>?';
// Удалите или добавьте символы по необходимости
```

### Изменить размер логов
**Файл:** `settings.py`
```python
'maxBytes': 1024 * 1024 * 10,  # 10 MB - измените на нужное значение
'backupCount': 10,  # Количество файлов - измените на нужное значение
```

## Безопасность

- ✅ Криптографически стойкая генерация (Web Crypto API)
- ✅ 16 символов (128 бит энтропии)
- ✅ Полное логирование всех операций
- ✅ Ротирующиеся логи (не переполняются)
- ✅ Идентификация администратора
- ✅ Временные метки в UTC

## Документация

- **EMPLOYEE_FORM_IMPROVEMENTS.md** - Полная документация функций
- **PASSWORD_GENERATION_GUIDE.md** - Руководство пользователя
- **PASSWORD_FEATURES_SUMMARY.md** - Краткое резюме
- **INSTALLATION_NOTES.md** - Этот файл (инструкции по установке)

## Поддержка

Для вопросов или проблем обратитесь к администратору системы.

---

**Версия:** 1.0  
**Дата:** 30 января 2026  
**Статус:** ✅ Готово к использованию

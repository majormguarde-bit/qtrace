# Улучшения формы создания сотрудника

## Что было добавлено

Форма создания/редактирования сотрудника теперь имеет расширенную функциональность для работы с паролем:

### 1. **Показать/Скрыть пароль** 👁️
- Кнопка с иконкой глаза рядом с полем пароля
- Нажатие переключает видимость пароля между точками и текстом
- Иконка меняется на перечеркнутый глаз при скрытом пароле

### 2. **Сбросить пароль** 🔄
- Кнопка со стрелкой обновления рядом с полем пароля
- Нажатие автоматически заполняет пароль значением логина
- Показывает галочку на 1.5 секунды в подтверждение действия

### 3. **Автозаполнение пароля при вводе логина** ⚡
- При вводе логина поле пароля автоматически заполняется тем же значением
- Работает только если поле пароля пусто
- Позволяет быстро создавать сотрудников с паролем по умолчанию

### 4. **Сгенерировать криптостойкий пароль** 🔐
- Кнопка со значком замка для генерации безопасного пароля
- Генерирует пароль длиной 16 символов с использованием криптографически стойкого генератора
- Включает прописные, строчные буквы, цифры и специальные символы
- Автоматически копирует пароль в буфер обмена
- Показывает уведомление с сгенерированным паролем
- Логирует все генерации паролей в журнал администратора

### 5. **Журнал администратора** 📋
- Все генерации паролей логируются локально в localStorage браузера
- Данные также отправляются на сервер для резервного копирования
- Логируется: время, администратор, сотрудник, действие
- Хранится до 100 последних записей в браузере
- Полный журнал сохраняется в файле `logs/admin_password_log.log`

### 6. **Информационное сообщение**
- При создании нового сотрудника показывается подсказка: "По-умолчанию пароль = логину"
- При редактировании: "Оставьте пустым, чтобы не менять"

## Обновленные файлы

### 1. **Шаблон формы**
**Файл:** `skkp_project/dashboard/templates/dashboard/employee_form.html`

### 2. **Backend логирования**
**Файл:** `skkp_project/dashboard/views.py`
- Добавлена функция `log_password_generation()` для логирования на сервере

### 3. **URL маршруты**
**Файл:** `skkp_project/dashboard/urls.py`
- Добавлен маршрут `api/log-password-generation/`

### 4. **Конфигурация логирования**
**Файл:** `skkp_project/config/settings.py`
- Добавлена конфигурация LOGGING для ротирующихся логов
- Логи сохраняются в `logs/admin_password_log.log`

## HTML структура

```html
<div class="input-group">
    {{ form.password }}
    <button class="btn btn-outline-secondary" type="button" 
            id="togglePasswordBtn" onclick="togglePasswordField()" 
            title="Показать/скрыть пароль">
        <i class="bi bi-eye" id="togglePasswordIcon"></i>
    </button>
    <button class="btn btn-outline-secondary" type="button" 
            id="resetPasswordBtn" onclick="resetPasswordField()" 
            title="Сбросить пароль">
        <i class="bi bi-arrow-clockwise"></i>
    </button>
    <button class="btn btn-outline-success" type="button" 
            id="generatePasswordBtn" onclick="generateSecurePassword()" 
            title="Сгенерировать криптостойкий пароль">
        <i class="bi bi-shield-lock"></i>
    </button>
</div>
```

## JavaScript функции

### togglePasswordField()
Переключает видимость пароля между текстом и точками

### resetPasswordField()
Заполняет пароль значением логина

### generateSecurePassword()
Генерирует криптостойкий пароль (16 символов) и логирует его
```javascript
function generateSecurePassword() {
    const length = 16;
    const charset = 'ABCDEFGHIJKLMNOPQRSTUVWXYZabcdefghijklmnopqrstuvwxyz0123456789!@#$%^&*()_+-=[]{}|;:,.<>?';
    let password = '';
    
    // Use crypto API for secure random generation
    const randomValues = new Uint8Array(length);
    crypto.getRandomValues(randomValues);
    
    for (let i = 0; i < length; i++) {
        password += charset[randomValues[i] % charset.length];
    }
    
    // ... логирование и уведомление
}
```

### logPasswordGeneration()
Логирует генерацию пароля в localStorage и отправляет на сервер

### sendLogToServer()
Отправляет логи на сервер для резервного копирования

## Использование

1. **Автозаполнение:** При вводе логина пароль автоматически заполняется тем же значением
2. **Показать пароль:** Нажмите на кнопку с иконкой глаза
3. **Сбросить пароль:** Нажмите на кнопку со стрелкой обновления
4. **Сгенерировать пароль:** Нажмите на кнопку со значком замка
5. **Просмотр журнала:** Откройте консоль браузера и выполните:
   ```javascript
   JSON.parse(localStorage.getItem('admin_password_logs'))
   ```

## Совместимость

- ✅ Bootstrap 5.3.0
- ✅ Bootstrap Icons 1.11.0
- ✅ Все современные браузеры (поддержка Web Crypto API)
- ✅ Мобильные устройства

## Безопасность

- ✅ Криптографически стойкая генерация (Web Crypto API)
- ✅ Пароли длиной 16 символов
- ✅ Включены спецсимволы для повышения стойкости
- ✅ Логирование всех операций с паролями
- ✅ Ротирующиеся логи (максимум 10 файлов по 10 МБ)

## Примечания

- Автозаполнение работает только если поле пароля пусто
- Функция "Сбросить пароль" работает только если заполнено поле логина
- При редактировании сотрудника можно оставить пароль пустым, чтобы не менять его
- Все функции работают без перезагрузки страницы
- Логи хранятся в `logs/admin_password_log.log` на сервере
- Локальные логи в браузере хранятся в localStorage (до 100 записей)

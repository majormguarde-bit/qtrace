# Улучшения поля пароля

## Что было добавлено

Все формы входа в системе теперь имеют расширенную функциональность для работы с паролем:

### 1. **Показать/Скрыть пароль**
- Кнопка с иконкой глаза (👁️) рядом с полем пароля
- Нажатие переключает видимость пароля между точками и текстом
- Иконка меняется на перечеркнутый глаз (👁️‍🗨️) при скрытом пароле

### 2. **Автозаполнение пароля**
- Добавлен атрибут `autocomplete="current-password"`
- Браузер может предложить сохраненный пароль
- Улучшает UX на мобильных устройствах

### 3. **Ссылка "Забыли пароль?"**
- Добавлена ссылка для восстановления пароля
- Расположена под полем пароля
- Готова к подключению функции восстановления

## Обновленные файлы

### 1. Dashboard Login (`skkp_project/dashboard/templates/dashboard/login.html`)
- Поле пароля обернуто в `input-group`
- Добавлена кнопка переключения видимости
- Добавлена ссылка восстановления пароля
- JavaScript функция `togglePassword()`

### 2. Superuser Login (`skkp_project/customers/templates/customers/superuser_login.html`)
- Поле пароля обернуто в `input-group`
- Добавлена кнопка переключения видимости
- Добавлена ссылка восстановления пароля
- JavaScript функция `togglePasswordAdmin()`

### 3. Mobile App (`skkp_project/mobile_app/index.html` + `app.js`)
- Поле пароля обернуто в `input-group`
- Добавлена кнопка переключения видимости
- Добавлена ссылка восстановления пароля
- JavaScript функция `togglePasswordMobile()` в `app.js`

## Технические детали

### HTML структура
```html
<div class="input-group">
    <input type="password" id="passwordInput" class="form-control" 
           required autocomplete="current-password">
    <button class="btn btn-outline-secondary" type="button" 
            onclick="togglePassword()">
        <i class="bi bi-eye" id="toggleIcon"></i>
    </button>
</div>
<div class="form-text">
    <a href="#" class="text-decoration-none">Забыли пароль?</a>
</div>
```

### JavaScript функция
```javascript
function togglePassword() {
    const input = document.getElementById('passwordInput');
    const icon = document.getElementById('toggleIcon');
    
    if (input.type === 'password') {
        input.type = 'text';
        icon.classList.replace('bi-eye', 'bi-eye-slash');
    } else {
        input.type = 'password';
        icon.classList.replace('bi-eye-slash', 'bi-eye');
    }
}
```

## Следующие шаги

1. **Восстановление пароля** - подключить функцию восстановления к ссылке "Забыли пароль?"
2. **Валидация пароля** - добавить требования к сложности пароля
3. **Двухфакторная аутентификация** - опционально добавить 2FA

## Совместимость

- ✅ Bootstrap 5.3.0
- ✅ Bootstrap Icons 1.11.0
- ✅ Все современные браузеры
- ✅ Мобильные устройства

# ⚠️ ТРЕБУЕТСЯ ПЕРЕЗАГРУЗКА ПРИЛОЖЕНИЯ

## Что было исправлено
Добавлен импорт `User` в `dashboard/views.py`, который был необходим для логирования создания сотрудников.

## Ошибка, которая была
```
NameError: name 'User' is not defined
```

Эта ошибка возникала при попытке логирования создания сотрудника или генерации пароля.

## Что нужно сделать

### Вариант 1: Автоматическая перезагрузка (Passenger)
Файл `tmp/restart.txt` был обновлен. Passenger автоматически перезагрузит приложение.

### Вариант 2: Ручная перезагрузка
Если приложение не перезагрузилось автоматически, выполните:

```bash
# Для Passenger
touch tmp/restart.txt

# Или перезагрузите веб-сервер
sudo systemctl restart apache2
# или
sudo systemctl restart nginx
```

## Проверка
После перезагрузки:

1. Создайте нового сотрудника через форму
2. Перейдите в "Администрирование" → "Журнал"
3. Должна появиться запись о создании сотрудника

## Коммит
- `75a181f` - fix: Add missing User import to views.py

## Статус
✅ Исправление готово, требуется перезагрузка приложения

# Руководство по генерации и логированию паролей

## Обзор

Система управления паролями сотрудников включает криптостойкую генерацию паролей и полное логирование всех операций.

## Функциональность

### 1. Генерация криптостойкого пароля

**Кнопка:** 🔐 (со значком замка)

**Характеристики:**
- Длина: 16 символов (> 8 требуемых)
- Использует Web Crypto API для криптографически стойкой генерации
- Включает: прописные буквы, строчные буквы, цифры, спецсимволы
- Автоматически копирует в буфер обмена
- Показывает уведомление с сгенерированным паролем

**Пример сгенерированного пароля:**
```
K7@mP2$xL9!qR4&vW
```

### 2. Логирование паролей

#### Локальное логирование (браузер)
- Хранится в `localStorage` под ключом `admin_password_logs`
- Максимум 100 последних записей
- Доступно через консоль браузера:
```javascript
JSON.parse(localStorage.getItem('admin_password_logs'))
```

#### Серверное логирование
- Все генерации отправляются на сервер
- Сохраняются в файл: `logs/admin_password_log.log`
- Ротирующиеся логи (максимум 10 файлов по 10 МБ)
- Также записываются в Django LogEntry для аудита

### 3. Информация в логе

Каждая запись содержит:
```json
{
  "timestamp": "2026-01-30T12:34:56.789Z",
  "username": "wb-worker-1",
  "password": "K7@mP2$xL9!qR4&vW",
  "action": "password_generated",
  "admin": "admin_username"
}
```

## Использование

### Шаг 1: Откройте форму создания сотрудника
```
Панель управления → Сотрудники → Добавить сотрудника
```

### Шаг 2: Заполните логин
```
Логин: wb-worker-1
```

### Шаг 3: Сгенерируйте пароль
Нажмите кнопку 🔐 "Сгенерировать криптостойкий пароль"

### Шаг 4: Пароль автоматически:
- ✅ Заполнится в поле пароля
- ✅ Скопируется в буфер обмена
- ✅ Будет залогирован в журнал администратора
- ✅ Покажет уведомление с паролем

### Шаг 5: Создайте сотрудника
Нажмите кнопку "Создать сотрудника"

## Просмотр журнала

### В браузере (консоль)
```javascript
// Получить все логи
const logs = JSON.parse(localStorage.getItem('admin_password_logs'));
console.table(logs);

// Получить последний лог
const lastLog = logs[logs.length - 1];
console.log(lastLog);

// Фильтр по сотруднику
const workerLogs = logs.filter(log => log.username === 'wb-worker-1');
console.table(workerLogs);
```

### На сервере
```bash
# Просмотр логов
tail -f logs/admin_password_log.log

# Поиск по сотруднику
grep "wb-worker-1" logs/admin_password_log.log

# Количество генераций
grep "password_generated" logs/admin_password_log.log | wc -l
```

## Безопасность

### Криптография
- ✅ Web Crypto API (стандарт W3C)
- ✅ Криптографически стойкий генератор случайных чисел
- ✅ 16 символов (128 бит энтропии)
- ✅ Полный набор символов (94 возможных)

### Логирование
- ✅ Все операции записываются
- ✅ Временные метки в UTC
- ✅ Идентификация администратора
- ✅ Ротирующиеся логи (не переполняются)

### Хранение
- ✅ Локальные логи в браузере (до 100 записей)
- ✅ Серверные логи в защищенной папке
- ✅ Django LogEntry для аудита

## Конфигурация

### Изменение длины пароля
В файле `skkp_project/dashboard/templates/dashboard/employee_form.html`:
```javascript
const length = 16;  // Измените на нужное значение
```

### Изменение набора символов
```javascript
const charset = 'ABCDEFGHIJKLMNOPQRSTUVWXYZabcdefghijklmnopqrstuvwxyz0123456789!@#$%^&*()_+-=[]{}|;:,.<>?';
// Удалите или добавьте символы по необходимости
```

### Изменение размера логов
В файле `skkp_project/config/settings.py`:
```python
'maxBytes': 1024 * 1024 * 10,  # 10 MB - измените на нужное значение
'backupCount': 10,  # Количество файлов - измените на нужное значение
```

## Устранение неполадок

### Пароль не копируется в буфер обмена
- Проверьте, что браузер поддерживает Clipboard API
- Убедитесь, что сайт работает по HTTPS (требование безопасности)

### Логи не сохраняются на сервере
- Проверьте, что папка `logs/` существует и доступна для записи
- Проверьте права доступа: `chmod 755 logs/`
- Проверьте логи Django: `python manage.py runserver`

### Логи не видны в браузере
- Откройте консоль браузера (F12)
- Выполните: `localStorage.getItem('admin_password_logs')`
- Если пусто, значит еще не было генераций

## Примеры

### Пример 1: Создание сотрудника с автогенерированным паролем
```
1. Логин: john-doe
2. Нажать 🔐 "Сгенерировать пароль"
3. Пароль: X9#kL2@pM5$vN8!qR
4. Создать сотрудника
5. Лог: [2026-01-30 12:34:56] Admin: admin | Employee: john-doe | Password Length: 16 chars
```

### Пример 2: Просмотр всех генераций за день
```javascript
const logs = JSON.parse(localStorage.getItem('admin_password_logs'));
const today = new Date().toISOString().split('T')[0];
const todayLogs = logs.filter(log => log.timestamp.startsWith(today));
console.table(todayLogs);
```

### Пример 3: Экспорт логов в CSV
```javascript
const logs = JSON.parse(localStorage.getItem('admin_password_logs'));
const csv = logs.map(log => 
  `${log.timestamp},${log.admin},${log.username},${log.action}`
).join('\n');
console.log(csv);
```

## Требования

- ✅ Современный браузер (Chrome, Firefox, Safari, Edge)
- ✅ JavaScript включен
- ✅ Web Crypto API поддерживается
- ✅ localStorage доступен
- ✅ Django 5.2+
- ✅ Python 3.8+

## Файлы

- `skkp_project/dashboard/templates/dashboard/employee_form.html` - Форма с кнопками
- `skkp_project/dashboard/views.py` - Backend логирования
- `skkp_project/dashboard/urls.py` - URL маршруты
- `skkp_project/config/settings.py` - Конфигурация логирования
- `logs/admin_password_log.log` - Файл логов (создается автоматически)

## Поддержка

Для вопросов или проблем обратитесь к администратору системы.

# 🚀 БЫСТРОЕ РАЗВЕРТЫВАНИЕ - Исправление импорта материалов

## ⚠️ КРИТИЧЕСКИЕ ИСПРАВЛЕНИЯ

### 1. Импорт материалов (КРИТИЧНО!)
Без этого исправления импорт шаблонов с материалами НЕ РАБОТАЕТ!

### 2. Фильтр шаблонов в форме задачи
В форме создания задачи теперь показываются только локальные шаблоны тенанта (не глобальные).

## 📋 Команды для копирования (qtrace.ru)

```bash
# Подключение к серверу
ssh s1147486@qtrace.ru

# Переход в проект и активация venv
cd ~/domains/qtrace.ru/qtrace && source .venv/bin/activate

# Получение изменений
git fetch origin && git checkout feature/diagram-cytoscape && git pull origin feature/diagram-cytoscape

# Перезапуск (миграции НЕ нужны!)
touch tmp/restart.txt

# Проверка логов
tail -f ~/logs/qtrace.ru/error.log
```

## ✅ Быстрая проверка

### Проверка 1: Импорт шаблонов
1. Откройте https://qtrace.ru
2. Войдите как суперпользователь
3. Типовые шаблоны → Экспорт любого шаблона
4. Импорт этого же файла
5. **Результат**: Должно работать без ошибки ValueError

### Проверка 2: Фильтр шаблонов в задачах
1. Войдите как администратор тенанта (например, https://wb.qtrace.ru)
2. Перейдите в "Задачи" → "Создать задачу"
3. Откройте выпадающий список "Выберите шаблон"
4. **Результат**: Должны отображаться только локальные шаблоны (не глобальные)

## 🐛 Что исправлено

### Исправление 1: Импорт материалов
- ❌ **Было**: `ValueError: Cannot assign DurationUnit to Material.unit`
- ✅ **Стало**: Импорт работает корректно с UnitOfMeasure

### Исправление 2: Фильтр шаблонов
- ❌ **Было**: В форме задачи показывались и глобальные, и локальные шаблоны
- ✅ **Стало**: Показываются только локальные шаблоны тенанта (template_type='local')

## 📝 Коммиты

```
3b47426 - Fix: Исправлена ошибка импорта материалов
b97e0bd - Docs: Документация по исправлению
5a399f9 - Docs: Итоговая сводка
338fc9d - Docs: Быстрая инструкция для развертывания
ea8ba0e - Fix: В форме создания задачи показываем только локальные шаблоны тенанта
```

## 📚 Подробная документация

- `MATERIALS_IMPORT_FIX.md` - техническое описание проблемы и решения импорта
- `FINAL_MATERIALS_FIX_SUMMARY.md` - полная сводка всех исправлений
- `HOSTING_DEPLOY_QTRACE.md` - общая инструкция по развертыванию

## 🔄 Откат (если нужно)

```bash
cd ~/domains/qtrace.ru/qtrace
git checkout c49e8d9  # Коммит до исправлений
touch tmp/restart.txt
```

## ⏱️ Время развертывания: ~2 минуты

Миграции БД не требуются - изменялась только логика!

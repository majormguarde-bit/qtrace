# 🚀 БЫСТРОЕ РАЗВЕРТЫВАНИЕ - Полный набор исправлений и новых функций

## ⚠️ КРИТИЧЕСКИЕ ИСПРАВЛЕНИЯ И НОВЫЕ ФУНКЦИИ

### 1. Импорт материалов (КРИТИЧНО!)
Без этого исправления импорт шаблонов с материалами НЕ РАБОТАЕТ!

### 2. Фильтр шаблонов в форме задачи
В форме создания задачи теперь показываются только локальные шаблоны тенанта (не глобальные).

### 3. Полное наследование этапов из шаблона (НОВАЯ ФУНКЦИЯ!)
При создании задачи из шаблона теперь наследуются ВСЕ данные этапов:
- Название этапа
- Длительность (в минутах и текстом)
- Должность исполнителя
- Материалы с количеством
- Порядок выполнения

### 4. Исполнители на уровне этапов (НОВАЯ ФУНКЦИЯ!)
- Исполнители теперь назначаются на уровне этапов, а не всей задачи
- Каждый этап может иметь своего исполнителя
- Поле `assigned_to` удалено из модели Task
- Добавлено поле `assigned_to` в модель TaskStage

## 📋 Команды для копирования (qtrace.ru)

```bash
# Подключение к серверу
ssh s1147486@qtrace.ru

# Переход в проект и активация venv
cd ~/domains/qtrace.ru/qtrace && source .venv/bin/activate

# Получение изменений
git fetch origin && git checkout feature/diagram-cytoscape && git pull origin feature/diagram-cytoscape

# ВАЖНО: Применение миграций БД (добавлены новые поля в TaskStage, удалено поле из Task)
python manage.py migrate tasks

# Перезапуск
touch tmp/restart.txt

# Проверка логов
tail -f ~/logs/qtrace.ru/error.log
```

## ✅ Быстрая проверка

### Проверка 1: Импорт шаблонов
1. Откройте https://qtrace.ru
2. Войдите как суперпользователь
3. Типовые шаблоны → Экспорт любого шаблона
4. Импорт этого же файла
5. **Результат**: Должно работать без ошибки ValueError

### Проверка 2: Фильтр шаблонов в задачах
1. Войдите как администратор тенанта (например, https://wb.qtrace.ru)
2. Перейдите в "Задачи" → "Создать задачу"
3. Откройте выпадающий список "Выберите шаблон"
4. **Результат**: Должны отображаться только локальные шаблоны (не глобальные)

### Проверка 3: Наследование этапов из шаблона
1. Войдите как администратор тенанта (например, https://wb.qtrace.ru)
2. Перейдите в "Задачи" → "Создать задачу"
3. Выберите шаблон из списка
4. **Результат**: 
   - Форма автоматически заполняется этапами из шаблона
   - Каждый этап содержит: название, длительность, должность, материалы
   - Материалы отображаются в формате "Название: количество единица"

### Проверка 4: Исполнители на уровне этапов
1. Войдите как администратор тенанта (например, https://wb.qtrace.ru)
2. Перейдите в "Задачи" → "Создать задачу"
3. Выберите шаблон или создайте задачу с нуля
4. **Результат**: 
   - Каждый этап имеет поле "Исполнитель"
   - Задача больше не имеет поля "Исполнитель"
   - Исполнители назначаются на уровне этапов

## 🐛 Что исправлено и добавлено

### Исправление 1: Импорт материалов
- ❌ **Было**: `ValueError: Cannot assign DurationUnit to Material.unit`
- ✅ **Стало**: Импорт работает корректно с UnitOfMeasure

### Исправление 2: Фильтр шаблонов
- ❌ **Было**: В форме задачи показывались и глобальные, и локальные шаблоны
- ✅ **Стало**: Показываются только локальные шаблоны тенанта (template_type='local')

### Новая функция 3: Полное наследование этапов
- ❌ **Было**: Наследовались только название и длительность
- ✅ **Стало**: Наследуются все данные: должность, материалы, текстовое описание длительности

### Новая функция 4: Исполнители на уровне этапов
- ❌ **Было**: Исполнитель назначался на уровне задачи
- ✅ **Стало**: Исполнители назначаются на уровне этапов (более гибко)

## 📝 Коммиты

```
3b47426 - Fix: Исправлена ошибка импорта материалов
b97e0bd - Docs: Документация по исправлению
5a399f9 - Docs: Итоговая сводка
338fc9d - Docs: Быстрая инструкция для развертывания
ea8ba0e - Fix: В форме создания задачи показываем только локальные шаблоны тенанта
79d59a3 - Feature: Реализована загрузка этапов из шаблона при создании задачи
8cd81a0 - Feature: Полное наследование этапов из шаблона (должность, материалы, длительность)
824a255 - Feature: Исполнители назначаются на уровне этапов, а не задач
```

## 🗄️ Миграции БД

**ВАЖНО**: В этом обновлении есть две миграции БД!

### Миграция 1: Добавление полей в TaskStage
Добавлены новые поля в модель `TaskStage`:
- `position_name` - название должности исполнителя (CharField)
- `duration_text` - текстовое описание длительности (CharField)
- `materials_info` - информация о материалах в формате JSON (JSONField)

Миграция: `tasks/migrations/0006_taskstage_duration_text_taskstage_materials_info_and_more.py`

### Миграция 2: Перемещение исполнителя на уровень этапов
- Удалено поле `assigned_to` из модели Task
- Добавлено поле `assigned_to` в модель TaskStage (ForeignKey на TenantUser)

Миграция: `tasks/migrations/0008_remove_task_assigned_to_taskstage_assigned_to.py`

## 📚 Подробная документация

- `MATERIALS_IMPORT_FIX.md` - техническое описание проблемы и решения импорта
- `FINAL_MATERIALS_FIX_SUMMARY.md` - полная сводка всех исправлений
- `HOSTING_DEPLOY_QTRACE.md` - общая инструкция по развертыванию

## 🔄 Откат (если нужно)

```bash
cd ~/domains/qtrace.ru/qtrace

# Откат миграций
python manage.py migrate tasks 0005

# Откат кода
git checkout 064d8a9  # Коммит до новых функций

touch tmp/restart.txt
```

## ⏱️ Время развертывания: ~3-5 минут

**Внимание**: Требуется применение двух миграций БД!
